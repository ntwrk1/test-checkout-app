# query RunInput {
#   discountNode {
#     metafield(namespace: "$app:shipping-discount", key: "function-configuration") {
#       value
#     }
#   }
# }

query RunInput {
  cart {
    deliveryGroups {
      id
      deliveryOptions {
        handle
        title
      }
      selectedDeliveryOption {
        handle
        title
      }
    }
  }
}
