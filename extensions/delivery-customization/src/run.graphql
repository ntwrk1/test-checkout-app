# query RunInput {
#   deliveryCustomization {
#     metafield(namespace: "$app:delivery-customization", key: "function-configuration") {
#       value
#     }
#   }
# }

query RunInput {
  cart {
    deliveryGroups {
      deliveryAddress {
        provinceCode
      }
      deliveryOptions {
        handle
        title
      }
    }
  }
}
